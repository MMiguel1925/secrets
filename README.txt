Numa arquitetura de um site comum (inclui bd) instalar via terminal:
npm i body-parser mongoose ejs express lodash


Criar o repositorio no GitHub e copiar para a inc abaixo:
digitar
1) $git remote add origin https://github.com/MMiguel1925/nome-repositorio.git
Este passo só é necessário a primeira vez quando usamos um novo repositorio.

Para usar o GitHub digitar na linha de comando:
git init
git add .
git commit -m "Add env variables"
git branch -M main
git push -u origin main



