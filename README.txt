Numa arquitetura de um site comum (inclui bd) instalar via terminal:
npm i body-parser mongoose ejs express lodash 
npm i bcrypt
Para instalar uma determinada versão digitar: npm i bcrypt@5.2.1



Criar o repositorio "nome-repositorio.git" no GitHub e copiar para a inc abaixo n:
digitar

Estes passo só é necessário a primeira vez quando usamos um novo repositorio.
Os pintos 1 e 2 só são necessários a primeira vez.

Para usar o GitHub digitar na linha de comando:
git init
git add .
git commit -m "Add env variables"
1) git branch -M main
2) git remote add origin https://github.com/MMiguel1925/nome-repositorio.git
git push -u origin main



