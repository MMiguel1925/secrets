Numa arquitetura de um site comum (inclui bd) instalar via terminal:
npm i body-parser mongoose ejs express lodash

Para usar o GitHub digitar na linha de comando:
git init
git add .
git commit -m "Add env variables"
git push -u origin main

